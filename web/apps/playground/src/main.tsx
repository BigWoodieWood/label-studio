import React from "react";
import { createRoot } from "react-dom/client";

const PlaygroundApp = () => <div>LabelStudio Playground coming soon...</div>;

const root = createRoot(document.getElementById("root")!);
root.render(<PlaygroundApp />);
